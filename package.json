{
    "name": "@hollymoon/common",
    "version": "0.3.2",
    "license": "MIT",
    "exports": {
        ".": {
            "import": "./lib/index.js",
            "require": "./lib/index.js",
            "types": "./lib/index.d.ts"
        }
    },
    "files": [
        "lib",
        "src",
        "package.json"
    ],
    "scripts": {
        "build": "rimraf lib/* && tsc -p tsconfig.build.json",
        "format": "prettier --write . --ignore-path .gitignore --ignore-path .prettierignore",
        "test": "vitest"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "rimraf": "^6.0.1",
        "typescript": "^5.7.2",
        "prettier": "^3.4.2",
        "vitest": "^2.1.8"
    }
}
